server:
  port: 8082
  address: 0.0.0.0

spring:
  application:
    name: flight-service

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false

eureka:
  client:
    enabled: false
